1. Para navegar entre páginas se necesita instalar React Navigation.
npm install @react-navigation/native

dependencias ->
npx expo install react-native-screens react-native-safe-area-context

stack navigator -> El Stack Navigator sirve para ir de una pantalla a otra (como páginas)
npm install @react-navigation/native-stack

Dentro de la carpeta src, se debe crear una carpeta navigation:
Dentro de la carpeta navigation crear AppNavigator.js



import { AntDesign, Feather, Ionicons, FontAwesome, MaterialIcons } from '@expo/vector-icons';
son todas las fuentes disponibles de iconos que podemos usar.

ejemplo:
<View style={{ marginTop: 0, marginBottom: 40 }}>
    <FontAwesome name="google" size={40} color="#DB4437" />
</View>